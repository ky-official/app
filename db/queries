DROP TABLE TASKS;

CREATE TABLE TASKS(
                      ID VARCHAR(10) NOT NULL CONSTRAINT ID PRIMARY KEY,
                      STATUS VARCHAR(100) NOT NULL CHECK (STATUS IN('QUEUED','RUNNING','FINISHED')),
                      PROGRESS INT NOT NULL CHECK (PROGRESS <=100 AND PROGRESS>=0),
                      MODIFIED DATETIME NOT NULL
);

select * from TASKS